/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/nextjs-indexeddb-dexiejs-starter',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
